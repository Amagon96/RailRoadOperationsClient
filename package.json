{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel --stream start"
  },
  "devDependencies": {
    "lerna": "^5.5.0"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  }
}
