{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel --stream start"
  },
  "devDependencies": {
    "lerna": "^5.5.0"
  }
}